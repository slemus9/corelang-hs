f = 3 ;
g x y = let z = x in z ;
h x = case (let y = x in y) of
      <1> -> 2 ;
      <2> -> 5 
      ;

fun1 x y = g (h x) f ;

fun2 x y = g (h x) (f + 2 * 5 >= 10 - 8 * 11 / 2) ;

fun3 a b xs = (a + (b - 12 / (b - a))) >= (length xs)
